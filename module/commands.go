//go:build ignore

// This legacy entry point was accidentally left in module/. The real
// application entry point is ../main.go. Keep this file excluded so the
// module package contains only package module files.
package main
